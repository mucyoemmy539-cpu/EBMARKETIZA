const wrap=document.getElementById("cartPage");
function renderCart(){if(!cart.length){wrap.innerHTML='<div class="empty"><h2>Your cart is empty</h2><a class="primary-btn inline" href="index.html">Continue Shopping</a></div>';return}let rows=cart.map(i=>{const p=PRODUCTS.find(x=>x.id===i.id);return `<div class="cart-item"><img src="${p.image}"><div class="cart-info"><h3>${p.name}</h3><p>${i.color?`Color: ${i.color}`:""} ${i.size?`| Size: ${i.size}`:""}</p><b>${money(p.price)}</b><div class="qty"><button onclick="changeQty('${i.key}',-1)">−</button><span>${i.qty}</span><button onclick="changeQty('${i.key}',1)">+</button></div></div><button class="remove" onclick="removeCart('${i.key}');renderCart()">Remove</button></div>`}).join("");wrap.innerHTML=`<div class="cart-items">${rows}</div><aside class="summary"><h2>Order Summary</h2><p>Subtotal <b>${money(subtotal())}</b></p><p>Delivery <b>${deliveryFee()?money(deliveryFee()):"Free"}</b></p><hr><p class="total">Total <b>${money(subtotal()+deliveryFee())}</b></p><a class="primary-btn inline" href="checkout.html">Checkout</a></aside>`}
renderCart();